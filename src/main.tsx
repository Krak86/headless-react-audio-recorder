import React from "react";
import ReactDOM from "react-dom/client";
import HARComponent from "./components/HARComponent";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <HARComponent />
  </React.StrictMode>
);
